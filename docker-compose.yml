services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - app-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production

  # --- Optional: uncomment to use PostgreSQL instead of SQLite ---
  # app:
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   environment:
  #     - DATABASE_URL=postgresql://sapphire:sapphire@db:5432/sapphire
  #
  # db:
  #   image: postgres:17-alpine
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: sapphire
  #     POSTGRES_PASSWORD: sapphire
  #     POSTGRES_DB: sapphire
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U sapphire"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # --- Optional: uncomment to use S3/R2 storage instead of local ---
  # app:
  #   environment:
  #     - STORAGE_PROVIDER=s3
  #     - S3_BUCKET=your-bucket
  #     - S3_REGION=auto
  #     - S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
  #     - S3_ACCESS_KEY_ID=your-key
  #     - S3_SECRET_ACCESS_KEY=your-secret
  #     - S3_PREFIX=

volumes:
  app-data:
  # pgdata:
